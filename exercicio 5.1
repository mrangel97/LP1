//ponteiros.c

#include<stdio.h>
#include<stdlib.h>

int main()
{
  int a,b,*p;

  printf("Digite um valor pra a:\n");
  scanf("%d",&a);
  printf("Digite um valor pra b:\n");
  scanf("%d",&b);
   
  if(a>=b)
    p=&a;
  else
    p=&b;

  *p=50-*p;

   printf("A:%d\nB:%d\n",a,b);
   return 0;
}
